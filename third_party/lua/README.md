# lua

Lua 5.4.8, the library sources only (no `lua.c` / `luac.c` / test hooks).
Used by `src/core/lua_host.*` to run plugins from the `plugins/` folder.

- source: https://www.lua.org (MIT license, see `LICENSE`)
- built as C, see `CMakeLists.txt` / `ceasta.vcxproj`
