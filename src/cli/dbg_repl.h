#pragma once

// ceasta-cli dbg <program> [args...]: an interactive terminal debugger on top of
// the debugger backend, with ceasta's names, disassembly and decompiler.
int cmd_dbg(int argc, char** argv);
